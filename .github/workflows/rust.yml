name: Rust

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest
    env:
      SHARING_DATA: ./SharingData.txt

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: cargo build --verbose
